# Robot Control Protocol

## 1. 项目结构中的协议分层

本项目包含两层协议：

1. **下位机串口协议**
   - 用于 Python 与 MCU 板之间通信
   - 定义控制命令、编码器复位、位置回传格式

2. **上位机动作输入协议**
   - 用于用户在终端中输入动作序列
   - 定义手指编号、时长编号和执行规则

---

## 2. 下位机串口协议

### 2.1 控制命令

Python 向单块板发送四路电机控制命令，格式如下：

```text
C:p1,p2,p3,p4
```

其中：

- `C:` 为控制命令前缀
- `p1, p2, p3, p4` 为四路电机 PWM 控制量
- 每一路为整数，正负号表示方向，绝对值表示驱动强度

示例：

```text
C:120,-80,0,30
```

表示：

- 电机1：正向 120
- 电机2：反向 80
- 电机3：停止
- 电机4：正向 30

### 2.2 编码器复位命令

Python 向单块板发送复位命令：

```text
R
```

作用：

- 将当前板上的编码器位置清零
- 通常用于重新标定或演示前初始化

### 2.3 位置回传命令

下位机向 Python 回传当前四路电机位置，格式如下：

```text
P:pos1,pos2,pos3,pos4
```

其中：

- `P:` 为位置回传前缀
- `pos1 ~ pos4` 为四路电机当前编码器计数值

示例：

```text
P:102,-56,0,300
```

表示当前四路电机位置分别为：

- 电机1：102
- 电机2：-56
- 电机3：0
- 电机4：300

---

## 3. 板级映射关系

系统默认包含两块控制板：

- **板子A**：控制小指、无名指
- **板子B**：控制中指、食指

默认映射如下：

### 板子A（小指 / 无名指）
- 电机0,1：小指
- 电机2,3：无名指

### 板子B（中指 / 食指）
- 电机0,1：中指
- 电机2,3：食指

每根手指默认由两个电机通道组成：

- `up`：上行/主拉通道
- `down`：下行/对向通道

具体目标位置由 `motor_config.json` 和 `preset_config.json` 管理。

---

## 4. 上位机动作输入协议

该协议用于 `robot_stdin.py`，在 VS Code 终端中直接输入动作序列。

### 4.1 基本格式

```text
手指序列 时长序列
```

两部分之间使用一个空格分隔。

示例：

```text
2345 1234
```

表示：

- 第1步：食指，持续1秒
- 第2步：中指，持续2秒
- 第3步：无名指，持续3秒
- 第4步：小指，持续4秒

### 4.2 手指编号定义

手指序列中只允许以下数字：

- `2`：食指
- `3`：中指
- `4`：无名指
- `5`：小指

注意：

- 当前协议中 **只允许 2 / 3 / 4 / 5**
- 不支持 `1`
- 不支持非数字字符

### 4.3 时长编号定义

时长序列中每一位表示对应步骤的持续时间，单位为秒。

允许范围：

- `0 ~ 9`

即每一步持续时间不超过 9 秒。

### 4.4 长度约束

- 手指序列长度必须与时长序列长度一致
- 两段都不能为空

合法示例：

```text
23 12
2345 1234
23232 23222
```

非法示例：

```text
23231 23222
```

原因：

- `1` 不是合法手指编号

非法示例：

```text
2345 123
```

原因：

- 前后长度不一致

---

## 5. 动作执行规则

对于终端输入的一组动作序列，默认执行规则如下：

1. 读取一位手指编号和一位时长编号
2. 将对应手指执行 `press`
3. 保持指定秒数
4. 将对应手指执行 `rest`
5. 进入下一步
6. 整组动作结束后，执行一次 `relax`

例如输入：

```text
234 123
```

执行逻辑为：

1. 食指 `press`，保持1秒，`rest`
2. 中指 `press`，保持2秒，`rest`
3. 无名指 `press`，保持3秒，`rest`
4. 全部结束后执行 `relax`

---

## 6. 支持的终端控制命令

除动作序列外，`robot_stdin.py` 还支持以下命令：

### 6.1 `relax`

```text
relax
```

作用：

- 所有手指回到松弛位
- 实际位置由 `preset_config.json` 中的 `relax` 决定

### 6.2 `reset`

```text
reset
```

作用：

- 向两块板发送编码器复位命令 `R`

### 6.3 `stop`

```text
stop
```

作用：

- 中断当前正在执行的动作序列
- 中断后程序会尝试执行 `relax`

### 6.4 `exit`

```text
exit
```

作用：

- 退出终端输入流程序
- 退出前会尝试执行 `relax` 并断开串口

---

## 7. 高层动作定义

在 Python 控制层中，常用的高层动作包括：

- `press`
- `rest`
- `preset`

### 7.1 `press` / `rest`

每根手指的 `press` 和 `rest` 目标位置定义在 `motor_config.json` 中。

示例：

```json
{
  "index": {
    "press": { "up": 5000, "down": -5000 },
    "rest": { "up": 0, "down": 0 }
  }
}
```

### 7.2 `preset`

多手指联合动作定义在 `preset_config.json` 中。

示例：

```json
{
  "relax": {
    "pinky":  { "up": 0, "down": 0 },
    "ring":   { "up": 0, "down": 0 },
    "middle": { "up": 0, "down": 0 },
    "index":  { "up": 0, "down": 0 }
  }
}
```

---

## 8. 注意事项

1. **不要同时运行 GUI 和终端输入流程序**
   - 两者都会占用同一组串口
   - 会导致串口冲突或连接失败

2. **手指动作的实际目标值来自配置文件**
   - 修改 `motor_config.json` 可以改变 `press/rest`
   - 修改 `preset_config.json` 可以改变 `relax` 等联合动作

3. **当前动作输入协议为串行单指演示**
   - 每一步只控制一个手指
   - 若后续需要双指/多指并发动作，应扩展新的输入协议
